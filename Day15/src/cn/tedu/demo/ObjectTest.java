package cn.tedu.demo;
/*
 * java.lang.Object类
 * 	1.Object是所有java类的根父类，
 * 	2.如果我们在声明类的时候，未继承任何类则默认父类为java.lang.Object
 * 	3.Object类的功能
 * 		属性：无
 * 		方法：
 * 			equals()/toString
 * 			getClass()/hashCode()
 * 			clone()/finalize()
 * 			wait()/notify()/notifyAll()
 * 	4.Object中只有一个无参构造方法
 */

public class ObjectTest {
	

}
